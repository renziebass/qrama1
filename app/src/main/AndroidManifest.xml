<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qrama">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Qrama"
        android:usesCleartextTraffic="true">
        <activity android:name=".teacher_camera"></activity>
        <activity android:name=".student_session" />
        <activity android:name=".teacher_session" />
        <activity android:name=".teacher_schedules" />
        <activity android:name=".student_camera" />
        <activity android:name=".student_main" />
        <activity android:name=".teacher_main" />
        <activity android:name=".teacher_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".student_login" />
    </application>

</manifest>